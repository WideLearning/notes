# relation
from [[algebraic concepts]]

a relation on set $A$ is a subset of it, or equally, a [[map]] from it to boolean values.
in some cases a relation is considered $n$-ary if $A = A_{1}\times A_{2} \dots \times A_{n}$.